/*
Starting from Windows 10 and forward, windows OS does not support emoji flags in chrome based browsers. Need to implement workaround this. An option is the restcountries.com API
 */

// export default function convertToEmoji(countryCode) {
//   const codePoints = countryCode
//     .toUpperCase()
//     .split("")
//     .map((char) => 0x1f1e6 + char.charCodeAt(0) - 65);
//   return String.fromCodePoint(...codePoints);
// }
